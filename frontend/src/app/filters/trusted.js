/**
 * Created by wrichtsfeld on 12.01.18.
 */

(function (angular) {
    'use strict';
    /*angular.module('apieval.filters')
        .filter('trusted', ['$sce', function ($sce) {
        return function(url) {
            var trust = $sce.trustAsResourceUrl(url);
            console.log("trusted filter:" + trust);
            return trust;
        };
    }]);*/
})(angular);
